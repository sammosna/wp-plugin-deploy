{
  "name": "wp-plugin-deploy",
  "version": "1.2.1",
  "bin": "./index.js",
  "description": "This script will update the version, build the plugin, zip it and upload it to the server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pnp": "pnpm version patch && pnpm publish --access public"
  },
  "keywords": [
    "wordpress"
  ],
  "author": "sammosna",
  "license": "ISC",
  "dependencies": {
    "archiver": "^5.3.1",
    "basic-ftp": "^5.0.2",
    "inquirer": "^8.0.0"
  }
}
